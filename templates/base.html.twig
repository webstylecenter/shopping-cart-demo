<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <style type="text/css">
                table { border: 1px solid black; border-spacing: 0}
                th { padding: 10px 5px; background-color: whitesmoke;}
                td {font-size: 15pt;}

                .categoryName {
                    font-size: 24pt;
                    color: darkblue;
                }

                .container {
                    width: 100%;
                }
                .left {
                    display: inline-block;
                    width: 45%;
                }
                .right {
                    float:right;
                    padding: 10px 20px;
                    width: 45%;
                    background-color: #ffbebe;
                }
                a.button {
                    display: inline-block;
                    border: 1px solid black;
                    background-color: whitesmoke;
                    padding: 5px;
                    color: black;
                    text-decoration: none;
                    width: 20px;
                    text-align: center;
                }
                a.button:hover {
                    background-color: gray;
                    cursor: pointer;
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <h1>Finetic WebShop</h1>
        <p><a href="{{ path('restart') }}">Herstart demo</a></p>

        <div class="container">
            <div class="left">
                {% block body %}{% endblock %}
            </div>
            <div class="right">
                {% include 'shoppingCart.html.twig' %}
            </div>
        </div>
    </body>
</html>
